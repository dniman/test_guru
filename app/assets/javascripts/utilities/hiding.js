/*document.addEventListener('turbolinks:load', function(){
  var control = document.querySelector('#badge_rule_type');

  if (control) { control.addEventListener('change', hideValueField) };
})

function hideValueField(){
  var value_field = document.querySelector("#rule_value");

  if (parseInt(this.value) > 1) {
    value_field.parentElement.classList.remove('hide');
  }
  else {
    value_field.parentElement.classList.add('hide');
    value_field.value = "";
  }
}
*/
